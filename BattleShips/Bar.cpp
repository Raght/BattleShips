#include "Bar.h"


Bar::Bar(olc::vi2d position, olc::vi2d size, float value, float max_value, float min_value)
	: position(position), size(size), value(value), maxValue(max_value), minValue(min_value)
{

}